fn main() {
    let x = 5;
    println!("Outer x = {}", x);

    {
        let x = 10;
        println!("Inner x = {}", x);
    }

    println!("Outer x after inner scope = {}", x);

    let x = 20;
    println!("Outer x after shadowing = {}", x);
}
